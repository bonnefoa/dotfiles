[Unit]
Description=Checks if paths that are currently being backed up have changed

[Path]
PathChanged=%h/notes/Passwords.kdbx
PathChanged=%h/notes/todo.txt
Unit=backup.service

[Install]
WantedBy=default.target
